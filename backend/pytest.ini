[pytest]
# Only the offline solver suite is collected by default. Everything else in
# backend/ needs live Earth Engine credentials (test_gee.py is a connectivity
# proof script, not a unit test), so a bare `pytest` must not try to run it.
testpaths = solver/tests
python_files = test_*.py
addopts = -q --strict-markers
